<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Maks223 | Engineering Profile</title>
    <!-- Шрифты -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:ital,wght@0,400;0,700;1,400&family=Space+Mono:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

    <style>
        :root {
            --bg-color: #E6E4DC; /* Теплый, бумажный оттенок */
            --ink-color: #1a1a1a; /* Основной цвет (мягкий черный) */
            --accent-gold: #b39b7d;
            --accent-gray: #7e7e7e;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            background-color: var(--bg-color);
            color: var(--ink-color);
            font-family: 'Space Mono', monospace;
            height: 100vh;
            overflow: hidden;
            display: flex;
            align-items: center;
            justify-content: center;
            position: relative;
            cursor: default;
        }

        /* --- ФОН И CANVAS --- */
        #networkCanvas {
            position: absolute;
            top: 0; left: 0;
            width: 100%; height: 100%;
            z-index: 1; /* Поднимаем слой с графикой выше, чтобы он был четким */
        }

        /* Шум теперь на заднем плане, чтобы не перекрывать точки */
        .noise-overlay {
            position: absolute;
            top: 0; left: 0; width: 100%; height: 100%;
            background: url('https://grainy-gradients.vercel.app/noise.svg');
            opacity: 0.35;
            pointer-events: none;
            z-index: 0; 
        }

        /* Сетка координат */
        .grid-lines {
            position: absolute;
            width: 100%; height: 100%;
            pointer-events: none;
            z-index: 2; /* Сетка поверх графики */
        }
        .grid-line {
            position: absolute;
            background-color: rgba(26, 26, 26, 0.06);
        }
        .v-line { top: 0; bottom: 0; width: 1px; left: 20%; }
        .v-line-2 { top: 0; bottom: 0; width: 1px; right: 20%; }
        .h-line { left: 0; right: 0; height: 1px; top: 15%; }
        .h-line-2 { left: 0; right: 0; height: 1px; bottom: 15%; }

        /* --- ГЛАВНЫЙ КОНТЕЙНЕР --- */
        .main-container {
            z-index: 10;
            display: grid;
            grid-template-columns: 1fr auto 1fr;
            align-items: center;
            width: 85%;
            max-width: 1200px;
            gap: 3rem;
            position: relative;
            /* Легкая подложка (размытие), чтобы текст читался поверх толстых линий */
            backdrop-filter: blur(2px); 
            border-radius: 20px;
            padding: 20px;
        }

        /* ЛЕВАЯ ЧАСТЬ */
        .hero-text {
            text-align: right;
            padding-right: 20px;
        }

        h1 {
            font-family: 'Playfair Display', serif;
            font-size: 6rem;
            line-height: 0.85;
            font-weight: 400;
            letter-spacing: -2px;
            color: var(--ink-color);
            margin-bottom: 15px;
            text-shadow: 2px 2px 0px var(--bg-color); /* Обводка для читабельности */
        }
        
        .role {
            font-size: 0.85rem;
            color: var(--ink-color);
            font-weight: 700;
            letter-spacing: 3px;
            text-transform: uppercase;
            border-bottom: 2px solid var(--accent-gold);
            padding-bottom: 5px;
            display: inline-block;
            background: rgba(230, 228, 220, 0.8);
        }

        /* ЦЕНТРАЛЬНАЯ ОСЬ */
        .center-axis {
            width: 2px;
            height: 350px;
            background: var(--ink-color);
            position: relative;
            display: flex;
            align-items: center;
            justify-content: center;
        }
        .center-axis::after {
            content: '223';
            background: var(--bg-color);
            padding: 10px 0;
            writing-mode: vertical-rl;
            font-size: 1.2rem;
            font-weight: 700;
            letter-spacing: 5px;
            color: var(--ink-color);
            border: 2px solid var(--ink-color);
        }

        /* ПРАВАЯ ЧАСТЬ - ССЫЛКИ */
        .links-wrapper {
            display: flex;
            flex-direction: column;
            gap: 15px;
            align-items: flex-start;
        }

        .meta-link {
            text-decoration: none;
            color: var(--ink-color);
            font-family: 'Playfair Display', serif;
            font-style: italic;
            font-size: 1.5rem;
            position: relative;
            padding: 15px 30px; 
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            border: 2px solid rgba(26,26,26, 0.1); 
            background: rgba(255,255,255,0.4); /* Полупрозрачный фон */
            width: 100%;
            max-width: 320px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            overflow: hidden; 
        }
        
        .meta-link span {
            z-index: 2; 
            position: relative;
        }

        .meta-link i {
            z-index: 2;
            font-size: 1.2rem;
            margin-left: 20px;
            opacity: 0.5;
            transition: opacity 0.3s;
        }

        .meta-link::before {
            content: '';
            position: absolute;
            top: 0; left: 0;
            width: 100%; height: 100%;
            background: var(--ink-color);
            z-index: 1;
            transform: translateX(-100%);
            transition: transform 0.4s cubic-bezier(0.645, 0.045, 0.355, 1);
        }

        .meta-link:hover {
            color: var(--bg-color);
            border-color: var(--ink-color);
            padding-left: 40px; 
        }

        .meta-link:hover::before { transform: translateX(0); }
        .meta-link:hover i { opacity: 1; color: var(--accent-gold); }

        /* --- ИНФОРМАЦИЯ ПО УГЛАМ --- */
        .tech-info {
            position: absolute;
            font-size: 0.7rem;
            color: #444; /* Темнее для контраста */
            z-index: 5;
            user-select: none;
            line-height: 1.6;
            font-weight: 700;
        }
        .t-top-left { top: 40px; left: 40px; border-left: 3px solid var(--accent-gold); padding-left: 15px; }
        .t-bottom-right { bottom: 40px; right: 40px; text-align: right; border-right: 3px solid var(--accent-gold); padding-right: 15px;}
        
        .running-numbers {
            position: absolute;
            top: 25%; right: 20px;
            font-family: monospace;
            color: rgba(26,26,26,0.3); /* Темнее */
            font-size: 0.8rem;
            width: 25px;
            word-break: break-all;
            line-height: 1;
            text-align: right;
            font-weight: bold;
        }

        /* АНИМАЦИИ */
        .reveal-box > * {
            animation: slideUp 1s cubic-bezier(0.77, 0, 0.175, 1) forwards;
            transform: translateY(110%);
        }
        @keyframes slideUp {
            from { transform: translateY(110%); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }

        /* MEDIA */
        @media (max-width: 850px) {
            .main-container {
                grid-template-columns: 1fr;
                text-align: center;
                gap: 2rem;
            }
            .hero-text { text-align: center; padding-right: 0; }
            h1 { font-size: 4rem; }
            .center-axis { width: 100%; height: 2px; margin: 10px 0; }
            .center-axis::after { writing-mode: horizontal-tb; padding: 0 15px; }
            .links-wrapper { width: 100%; align-items: center; }
            .meta-link { text-align: center; justify-content: center; background: rgba(255,255,255,0.8); }
        }
    </style>
</head>
<body>

    <!-- ЗАДНИЙ ФОН: CANVAS ВАЖНЕЕ ВСЕГО -->
    <div class="noise-overlay"></div> <!-- Шум снизу -->
    <canvas id="networkCanvas"></canvas> <!-- Canvas сверху шума -->
    
    <div class="grid-lines">
        <div class="grid-line v-line"></div>
        <div class="grid-line v-line-2"></div>
        <div class="grid-line h-line"></div>
        <div class="grid-line h-line-2"></div>
    </div>

    <!-- ТЕХНИЧЕСКИЕ ДАННЫЕ -->
    <div class="tech-info t-top-left">
        <div>NET_STATUS: ACTIVE</div>
        <div id="clock">00:00:00</div>
        <div>RENDER: HIGH_DENSITY</div>
    </div>

    <div class="tech-info t-bottom-right">
        <div>STACK: HTML5 / CANV.JS</div>
        <div>VER: 3.1.BOLD</div>
        <div>© MAKS223</div>
    </div>

    <div class="running-numbers" id="numCol">010010<br>110001<br>001010</div>

    <!-- ОСНОВНОЙ КОНТЕНТ -->
    <div class="main-container">
        <!-- ТЕКСТ -->
        <div class="hero-text">
            <div class="reveal-box">
                <h1>MAKS</h1>
            </div>
            <div class="reveal-box" style="animation-delay: 0.1s">
                <span class="role">Architect / Developer</span>
            </div>
        </div>

        <!-- ОСЬ -->
        <div class="center-axis"></div>

        <!-- КНОПКИ -->
        <div class="links-wrapper">
            <div class="reveal-box" style="animation-delay: 0.3s; width: 100%; display:flex; justify-content: center;">
                <a href="https://t.me/ValuevMV223" target="_blank" class="meta-link">
                    <span>Telegram</span>
                    <i class="fa-brands fa-telegram"></i>
                </a>
            </div>
            <div class="reveal-box" style="animation-delay: 0.4s; width: 100%; display:flex; justify-content: center;">
                <a href="https://github.com/Maks223-web" target="_blank" class="meta-link">
                    <span>GitHub Repos</span>
                    <i class="fa-brands fa-github"></i>
                </a>
            </div>
        </div>
    </div>

    <script>
        const canvas = document.getElementById('networkCanvas');
        const ctx = canvas.getContext('2d');
        canvas.width = window.innerWidth;
        canvas.height = window.innerHeight;

        let particlesArray;

        // --- КЛАСС ЧАСТИЦ (ИЗМЕНЕНИЯ: ЦВЕТ И РАЗМЕР) ---
        class Particle {
            constructor(x, y, directionX, directionY, size) {
                this.x = x;
                this.y = y;
                this.directionX = directionX;
                this.directionY = directionY;
                this.size = size;
            }
            draw() {
                ctx.beginPath();
                ctx.arc(this.x, this.y, this.size, 0, Math.PI * 2, false);
                
                // Сделали цвет темно-серым (почти черным), чтобы было хорошо видно
                ctx.fillStyle = '#2c2c2c'; 
                ctx.fill();
            }
            update() {
                // Отталкивание от краев
                if (this.x > canvas.width || this.x < 0) this.directionX = -this.directionX;
                if (this.y > canvas.height || this.y < 0) this.directionY = -this.directionY;

                this.x += this.directionX;
                this.y += this.directionY;
                this.draw();
            }
        }

        function init() {
            particlesArray = [];
            let numberOfParticles = (canvas.width * canvas.height) / 11000;
            
            for (let i = 0; i < numberOfParticles; i++) {
                // --- ИЗМЕНЕНИЕ 1: Размер от 3px до 7px (было до 2px) ---
                let size = (Math.random() * 4) + 3; 
                let x = (Math.random() * ((innerWidth - size * 2) - (size * 2)) + size * 2);
                let y = (Math.random() * ((innerHeight - size * 2) - (size * 2)) + size * 2);
                
                // Скорость движения чуть меньше для массивности
                let directionX = (Math.random() * 0.4) - 0.2; 
                let directionY = (Math.random() * 0.4) - 0.2; 
                
                particlesArray.push(new Particle(x, y, directionX, directionY, size));
            }
        }

        function animate() {
            requestAnimationFrame(animate);
            ctx.clearRect(0, 0, innerWidth, innerHeight);
            
            for (let i = 0; i < particlesArray.length; i++) {
                particlesArray[i].update();
            }
            connect();
        }

        // --- ЛОГИКА СОЕДИНЕНИЙ (ИЗМЕНЕНИЯ: ТОЛЩИНА И ПРОЗРАЧНОСТЬ) ---
        function connect() {
            for (let a = 0; a < particlesArray.length; a++) {
                for (let b = a; b < particlesArray.length; b++) {
                    let distance = ((particlesArray[a].x - particlesArray[b].x) ** 2) + 
                                   ((particlesArray[a].y - particlesArray[b].y) ** 2);
                    
                    // Расстояние соединения (стало чуть меньше, чтобы не было "грязи")
                    if (distance < (canvas.width/8) * (canvas.height/8)) {
                        
                        let opacity = 1 - (distance / 20000);
                        
                        // Цвет линий стал темно-серым
                        ctx.strokeStyle = 'rgba(30, 30, 30, ' + opacity * 0.5 + ')'; 
                        
                        // --- ИЗМЕНЕНИЕ 2: Толщина линии 2px (было 1px) ---
                        ctx.lineWidth = 2; 
                        
                        ctx.beginPath();
                        ctx.moveTo(particlesArray[a].x, particlesArray[a].y);
                        ctx.lineTo(particlesArray[b].x, particlesArray[b].y);
                        ctx.stroke();
                    }
                }
            }
        }

        window.addEventListener('resize', () => {
            canvas.width = innerWidth;
            canvas.height = innerHeight;
            init();
        });
        
        init();
        animate();

        // Часы и цифры
        setInterval(() => { document.getElementById('clock').innerText = new Date().toLocaleTimeString(); }, 1000);
        
        const numCol = document.getElementById('numCol');
        setInterval(() => {
            let str = '';
            for(let i=0; i<25; i++) str += Math.round(Math.random()) + ((i%6===0) ? '<br>' : '');
            numCol.innerHTML = str;
        }, 150);

    </script>
</body>
</html>
